/* HEADER — comportamento global do cabeçalho: busca, alertas e URLs */
import {alerts as loadAlerts} from '../core/data.js';import {escapeHtml} from '../core/utils.js';import {pageUrl,assetUrl} from '../core/paths.js';
export {pageUrl,assetUrl};
export function initHeader(){document.querySelectorAll('input[placeholder="Buscar conteúdos..."]').forEach(input=>input.addEventListener('keydown',e=>{if(e.key==='Enter'&&input.value.trim())location.href=pageUrl(`paginas/catalogo/?q=${encodeURIComponent(input.value.trim())}`)}));const bell=document.querySelector('.tool-icon[aria-label="Notificações"]');if(!bell)return;let panel=document.getElementById('nexoAlerts');if(!panel){panel=document.createElement('div');panel.className='nexo-alert-panel';panel.id='nexoAlerts';panel.innerHTML='<h3>Informações importantes</h3><div id="nexoAlertList">Carregando...</div>';document.body.appendChild(panel)}bell.onclick=async()=>{panel.classList.toggle('open');if(!panel.classList.contains('open'))return;try{const data=await loadAlerts();const active=(data.alertas||[]).filter(a=>a.ativo!==false);document.getElementById('nexoAlertList').innerHTML=active.length?active.map(a=>`<div class="nexo-alert"><b>${escapeHtml(a.titulo)}</b><p>${escapeHtml(a.mensagem)}</p><small>${escapeHtml(a.data||'')}</small></div>`).join(''):'<div class="nexo-alert">Nenhuma informação nova.</div>'}catch{document.getElementById('nexoAlertList').textContent='Não foi possível carregar os alertas.'}}}
